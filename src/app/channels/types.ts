export interface MusicType {
    title: string;
    coverImage: {
      asset: {
        _id: string;
        url: string;
      };
    };
    videoFile: {
      asset: {
        _id: string;
        url: string;
      };
    };
}

export interface CookingType {
    title: string;
    coverImage: {
      asset: {
        _id: string;
        url: string;
      };
    };
    videoFile: {
      asset: {
        _id: string;
        url: string;
      };
    };
}
  
  